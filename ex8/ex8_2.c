#include <stdio.h>

int main(int argc, char *argv[]){
	char none[] = "";	
	char one[] = "a";

	printf("The size of 'none': %ld.\n", sizeof(none));
	printf("The size of 'one': %ld.\n", sizeof(one));
}
